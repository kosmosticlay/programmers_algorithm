function solution(arr) {
    const result = [];
    arr.forEach((el) => {
        let count = el;
        for(let i = 0; i < el; i++) {
            result.push(el)
        }
    })
    return result;
}